#include "main.h"
/**
 * mul - multiple two parameters numbers
 * @a: first number to mulptiplie
 * @b: second parameter of multiple
 *
 * Return: value of multiple
 */
int mul(int a, int b)
{
	return (a * b);
}

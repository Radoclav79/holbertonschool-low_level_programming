#include "main.h"
#include <stdio.h>
/**
 * _abs - compute absolute value
 * @n: check value
 *
 * Return: abs value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}

#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - string length
 * @s: parameter
 *
 * Return: lenght
 */
int _strlen(char *s)
{
	int len;
	
	len = strlen(s);
	printf("%d\n", len);
	return (0);
}
